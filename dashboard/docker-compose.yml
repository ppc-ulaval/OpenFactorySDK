services:
  dashboard:
    build: .
    networks:
      - factory-net
    ports:
      - "3000:3000"
    volumes:
      - .:/app
    working_dir: /app
    command: bash -c "python3 app.py"
    stdin_open: true
    tty: true

networks:
  factory-net:
    name: factory-net
    external: true